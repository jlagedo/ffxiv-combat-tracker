using Avalonia.Controls;

namespace Fct.SamplePlugin.Views;

// Code-behind. InitializeComponent() is generated from the .axaml by the Avalonia XAML compiler; the
// `partial` keyword is what lets the generated half join this class. Keep code-behind this thin —
// all state and logic live in the view model.
public partial class MonitorView : UserControl
{
    public MonitorView() => InitializeComponent();
}
