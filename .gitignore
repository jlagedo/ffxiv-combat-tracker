# Third-party proprietary binaries — local build inputs only, never redistribute
/lib/

# Machine-local build/memory overrides — per-developer paths to real third-party ACT binaries
# used only as build/test inputs; never committed.
Directory.Build.local.props
CLAUDE.local.md

# .NET build artifacts
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
.vs/
artifacts/

# Distributable builds from build/ (dotnet run --project build [-- release])
/dist/

# Tooling
*.binlog
TestResults/

# Raw personal FFXIV network logs (never commit; copied here only as a local capture source)
tests/fixtures/FFXIVLogs/

# Local mass-compare outputs (per-file oracle/ours TSVs + reports; carry real combatant names)
tmp/
tools/mass-compare/bin/
tools/mass-compare/obj/
